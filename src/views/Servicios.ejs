<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        

        /* Contenedor principal centrado */
        .container {
            display: flex;
            justify-content: center; /* Centra horizontalmente */
            align-items: center; /* Centra verticalmente */
            
        }

        iframe {
            
            border-radius: 15px;
            box-shadow: 7px 4px 8px rgba(0, 0, 0, 0.2);
            width: 750px;
            height: 750px;
        }
    </style>
</head>
<body>
    <!-- Navegación -->
    <%- include('partials/navigation') %>

    <!-- Contenedor centrado con el iframe -->
    <div class="container">
        <iframe
        allow="microphone;"
        width="350"
        height="430"
        src="https://console.dialogflow.com/api-client/demo/embedded/9e51899c-f008-469e-9bd1-c4db50e85eaa">
</iframe>
    </div>

    <!-- Pie de página -->
    <%- include('partials/footer') %>
</body>
</html>